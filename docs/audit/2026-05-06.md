# Audit – 2026-05-06

> Static-only audit. No live dev server, no DB, no persona walks. "Slow" findings are pattern-based (Prisma include shape, missing `take`, sequential awaits) rather than empirically measured.

## Previous findings recheck

Source digest: `docs/audit/2026-05-03.md` (f1–f29).

| ID | Status | Notes |
|---|---|---|
| f1 | Fixed | `GET` is exported in `app/api/admin/practice/[id]/route.ts:20`. |
| f2 | Still present | `createSchema` in `app/api/admin/competitions/route.ts:24-32` still omits `division`; UI form has no division input. |
| f3 | Still present | `app/dashboard/competitions/[id]/page.tsx:105` still renders `isPublished` as a display-only badge with no toggle. |
| f4 | Fixed | `app/api/member/hub/route.ts:345-346` returns `?? 0` for hour aggregates. |
| f5 | Partial | `app/dashboard/members/page.tsx` uses `select` (not `include`) but still ships **all** members with no `take` limit and no client search. The admin API supports pagination but the page does not consume it. |
| f6 | Partial | Confirmed last session as -28% / -14%. Detail page now uses `Promise.all`, but `competition-roster-manager.tsx` is still 586 lines and does deep includes via `listCanonicalCompetitionRosters`. |
| f7 | Partial | `lib/practice-assessments.ts:374` still has `take: 20` on attempts. The 1058ms regression flagged last session needs an empirical re-run. |
| f8 | Fixed | `lib/rate-limit.ts:155` `rateLimitRetryAfterSeconds` is no longer exported. |
| f9 | Still present | `lib/practice-assessments.ts` is now 1100 lines (was 1088). |
| f10 | Still present | `competition-roster-manager.tsx` still 586 lines. |
| f11 | Fixed | `app/api/admin/hours/route.ts:34-62` accepts `entryIds[]` for bulk approve/reject. |
| f12 | Still present | `app/dashboard/hours/page.tsx:23` filters to `status: "PENDING"`; no history tab. |
| f13 | Still present | `app/dashboard/members/page.tsx` still has no search input; `MembersTable` has no filter. |
| f14 | Still present | Tied to f2. |
| f15 | Still present | `app/api/member/hours/route.ts` exports only `GET` and `POST`. |
| f16 | Still present | No `app/api/member/events/[id]/enroll` route. |
| f17 | Still present | No auto-population of competition rosters. |
| f18 | Fixed | `app/api/member/forms/route.ts:38-73` now exports `PATCH` (acknowledge/upload). |
| f19 | Still present | `submittedAt` not rendered in `admin-hours-view.tsx`. |
| f20 | Still present | `approvedBy` / `approvedAt` not surfaced anywhere. |
| f22 | Still present | `z.nativeEnum(AssessmentPartType)` still gives the generic message. |
| f23 | Still present | Hour entry form has no autosave. |
| f24 | Still present | `member-hours-view.tsx` has no status filter. |
| f25 | Still present | `categoryTargets[]` is computed in hub but never rendered. |
| f26 | Still present | `admin-hours-view.tsx:181-195` still omits submission date and email. |
| f27 | Still present | `app/dashboard/competitions/page.tsx:60-62` renders raw `comp.type`; no `TYPE_LABELS`. |
| f28 | Still present | `competition-roster-manager.tsx:427` still says "No canonical competition rosters yet." |
| f29 | Still present | No "talk to your admin" copy on the empty member competition view. |

Open backlog count: **22** (f2, f3, f5, f6, f7, f9, f10, f12, f13, f14, f15, f16, f17, f19, f20, f22, f23, f24, f25, f26, f27, f28, f29).

---

## New findings

### P1 — Broken (0)

(None new this run.)

### Track A — Slow (6)

- (f30) `app/api/admin/dues/route.ts:21-24` — `include: { memberSeason: { include: { user: { select: ... } } } }` ships every text-heavy column on `MemberSeason` (`whyJoin`, `contributionIdeas`, `awards`, `previousEvents`, `scienceClasses`, `mathClasses`, `questions`, `notes`, `statusReason`) for every invoice in the season. Switch the inner `include` to a `select` of `id` + the user fields actually rendered. — static
- (f31) `app/api/admin/hours/route.ts:23-28` — same shape; the GET route returns *all* hour entries for the season with the full `MemberSeason` row joined. — static
- (f32) `app/api/admin/forms/[id]/submissions/route.ts:21-25` — same shape on `formSubmission.findMany`; payload size scales with member text-bloat. — static
- (f33) `app/api/admin/club-events/[id]/attendance/route.ts:31-36` and `:124-128` — two queries with the same full-`MemberSeason` shape. — static
- (f34) `app/dashboard/finances/page.tsx:23-25` — same pattern, server component ships all the text columns into the client bundle. `InvoicesTable` only consumes `id` + `user.{id,firstName,lastName,email}`. — static
- (f35) `app/dashboard/hours/page.tsx:24-27` — same pattern in the admin pending-queue query; consumer (`AdminHoursView`) only reads `memberSeason.user.firstName/lastName`. — static

### Track A — Dead-code (0)

Spot-checked `lib/format.ts`, `lib/email-domains.ts`, `lib/competition-event-sync.ts`, `lib/ui-preferences.ts`, `lib/api.ts`, and the practice-assessments helpers. All exports have at least one consumer in `app/`, `components/`, `lib/`, or `scripts/`. Nothing safe to delete this run.

### Track A — Refactor (2 deferred)

- (f9, carried) `lib/practice-assessments.ts` — 1100 lines. Needs human design (file split would touch every importer); deferred.
- (f10, carried) `competition-roster-manager.tsx` — 586 lines. Same; deferred.

### Track B — Missing (0 new)

The major Missing items remain f15 (member edit/delete own hours), f16 (member self-enroll in events), f17 (auto-populate rosters). f15 is the cleanest fit for a single-PR feature this run.

### Track B — UX (1)

- (f36) `app/dashboard/competitions/page.tsx:60-62` — type badge renders raw enum (`INVITATIONAL`, etc.); same shape on the detail page header (`competitions/[id]/page.tsx:104`). Same pattern as f27 — a `TYPE_LABELS` map would humanize both. — static

### Track B — UI (1)

- (f37) `competition-roster-manager.tsx:453,456,496,499` and `events-manager.tsx:120,123` — icon-only edit/delete buttons (`<Button size="icon-sm">` with only an `IconEdit`/`IconTrash`) have no `aria-label`. Screen readers announce them as unlabeled. The same pattern recurs in `admin-hours-view.tsx:198,201` (single-row approve/reject). — static

---

## Triage policy

Auto-mechanical: P1 + Slow + Dead-code + contained Refactor on `audit-loop`. Track B (Missing + UX/UI) on `audit-loop-features` with one Missing implementation and small contained polish.

This run:
- Track A: f30, f31, f32, f33, f34, f35 (Slow → `include`-to-`select`).
- Track B: f15 (Missing — member edit/delete own hours), plus f27/f36 (humanized type labels) and f28 (empty-state copy) as small contained UX polish.

Refactor (f9, f10) deferred — needs human design.

---

## Before / After

Static-only — no empirical timing this run. Each Track A entry below lists the route, the pre-fix pattern, and the change applied.

| Finding | Route / file | Pattern present | Change applied |
|---|---|---|---|
| f30 | `app/api/admin/dues/route.ts` GET | `memberSeason: { include: { user: { select: ... } } }` ships full `MemberSeason` row | Inner `include` → `select { id, user: { select: ... } }` |
| f31 | `app/api/admin/hours/route.ts` GET | same | same |
| f32 | `app/api/admin/forms/[id]/submissions/route.ts` GET | same | same |
| f33 | `app/api/admin/club-events/[id]/attendance/route.ts` GET + POST result | same (twice) | same (both) |
| f34 | `app/dashboard/finances/page.tsx` | same | same |
| f35 | `app/dashboard/hours/page.tsx` admin | same | same |

Net effect (pattern-based): each of these queries previously joined every `MemberSeason` text column (≈10 free-text fields including the original application essays) per row. After the change they ship `{ id, user: { id, firstName, lastName[, email] } }` only.

Track B in the second PR adds member self-service for hour edits; see that PR description for what to manually verify.
